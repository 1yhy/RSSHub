import type { Namespace } from '@/types';

export const namespace: Namespace = {
    name: 'MCP.so',
    url: 'mcp.so',
    description: `
:::tip
MCP.so is a community for sharing and discovering Model, Claude, and Playground (MCP) servers and clients.
:::`,
};
